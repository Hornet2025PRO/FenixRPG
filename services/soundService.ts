
type SoundType = 'confirm' | 'damage' | 'start' | 'ui_click';

// Los sonidos han sido deshabilitados según la solicitud del usuario.
export const playSound = (type: SoundType): void => {
  // Esta función se deja en blanco intencionalmente para deshabilitar todos los efectos de sonido.
};
